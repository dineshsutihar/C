// program to use string function
#include<stdio.h>
#include<string.h>
int main()
{
    char str[20];
    int l;
    printf("\n enter a sting:");
    scanf("%s", &str);
    l=strlen(str);
    printf("\n length of a string =%d",l);

}
