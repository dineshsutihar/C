#include <stdio.h>

 void main()
{
 //if statement
   char first,last;
    printf("enter your first name:");
    scanf("%c",&first);
    printf("enter your last name: ");
    scanf("%c",&last);
    printf("\n your full name is :%c %c", first,last);
   /* if (x < y)
    {
        printf("x is less than y");
    }
    else 
    {
        printf("y is less than x");
    }
    return 0;
    */
}
